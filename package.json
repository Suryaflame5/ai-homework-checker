{
  "name": "ai-homework-checker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx esbuild index.ts --bundle --platform=node --outdir=dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.11.0",
    "express": "^4.19.2",
    "esbuild": "^0.21.0"
  }
}
